{
  "name": "ros2_lerobot_dev",
  "dockerComposeFile": "../ROS2_environment/docker-compose.yml",
  "service": "ros2_dev",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached"
  ],
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose"
}